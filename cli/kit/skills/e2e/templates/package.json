{
  "private": true,
  "scripts": {
    "bddgen": "npx bddgen",
    "test": "npx bddgen && npx playwright test --project=flows",
    "test:spends": "npx bddgen && npx playwright test --project=flows",
    "bdd:report": "npx http-server reports -o business-flows.html"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "playwright-bdd": "^9.2.0",
    "http-server": "^14.1.1"
  }
}
